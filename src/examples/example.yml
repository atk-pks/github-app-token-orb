description: >
  Example usage of the github-app-token orb.
usage:
  version: 2.1
  orbs:
    github-app-token: rossywhite/github-app-token@0.0.1
  executors:
    default:
      docker:
        - image: cimg/deploy:2022.11
  jobs:
    generate-token:
      executor: default
      steps:
        - checkout
        - github-app-token/generate-token
  workflows:
    use-my-orb:
      jobs:
        - generate-token
